<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.application.mwth2019kotlin">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ukm.bizops.AlurkasActivity">
        </activity>
        <activity android:name=".kasir.order.OrderKasirActivity"/>
        <activity android:name=".ukm.order.OrderUkmActivity"/>
        <activity android:name=".kasir.MainKasirActivity"/>
        <activity android:name=".ukm.MainUkmActivity"/>
        <activity android:name=".ukm.product.add.ProductAddActivity"/>
        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".register.RegisterActivity"/>
        <activity android:name=".register.OnboardingActivity"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".kasir.invoice.KasirInvoiceActivity"/>
        <activity android:name=".kasir.checkout.CheckoutActivity"/>
        <activity android:name=".landing.LandingActivity"/>
    </application>

</manifest>